apiVersion: v1
kind: Pod
metadata:
  name: myapp
  labels:
    app: myapp
spec:
  containers:
  - name: app-cont
    image: meryemya/my-app:1.0.0
    imagePullPolicy: Always
    ports:
     - containerPort: 8083
